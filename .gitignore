# Virtual environment — recreate with `uv sync`
.venv/

# Python bytecode
__pycache__/

# Corpus: HashiCorp terraform-provider-aws docs (MPL-2.0). Not redistributed
# here — fetch them with `uv run python local/00_fetch_corpus.py` (see README).
local/corpus/

# Derived pipeline artifacts — regenerate by running the numbered tasks in order
local/chunks.jsonl
local/*.faiss
local/*.npy
