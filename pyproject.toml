[project]
name = "rag-project"
version = "0.1.0"
description = "Local-first RAG pipeline over AWS/Terraform docs (Ollama + FAISS), later ported to AWS"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "faiss-cpu>=1.14.3",
    "numpy>=2.5.0",
]
